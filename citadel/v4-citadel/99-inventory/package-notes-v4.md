# Package Notes

This zip is a full version snapshot, not a patch.

Per the current project rule:
- A new version zip must include the full set of files for that version.
- It must not include only changed files.
- Each version should stand on its own.

This v4 package includes all currently reconstructed details available from the active conversation and project memory.
